import { FrontOfficePipe } from './front-office.pipe';

describe('FrontOfficePipe', () => {
  it('create an instance', () => {
    const pipe = new FrontOfficePipe();
    expect(pipe).toBeTruthy();
  });
});
